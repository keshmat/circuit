---
import { Image } from "astro:assets";
import { Fragment } from "astro/jsx-runtime";
import keshmatLogo from "../assets/keshmat-logo.png";
import headerImage from "../assets/Header.jpg";
---

<div id="container">
  <main>
    <section
      id="hero"
      class="hero min-h-[60vh] sm:min-h-[70vh] md:min-h-[80vh] pt-2 sm:pt-12"
    >
      <div class="hero-content text-center px-2 sm:px-6">
        <div class="max-w-2xl">
          <div class="hidden sm:block">
            <div class="flex flex-row items-center justify-center gap-4">
              <Image
                src={keshmatLogo}
                alt="Keshmat Logo"
                class="w-12 sm:w-14 md:w-16"
                width={64}
                height={64}
              />
              <h1 class="text-3xl sm:text-4xl md:text-5xl font-bold">
                Keshmat Chess Circuit
              </h1>
            </div>
          </div>
          <p class="py-4 sm:py-6 text-base sm:text-lg">
            The Keshmat Chess Circuit is a series of FIDE rated rapid chess
            tournaments held throughout the year. Open to players of all ages
            and skill levels.
          </p>
          <Image
            src={headerImage}
            alt="Chess tournament scene"
            class="w-full max-w-3xl mx-auto"
            width={1200}
            height={800}
          />
          <div
            class="flex flex-col sm:flex-row items-center justify-center gap-2 mt-4"
          >
            <a
              role="button"
              href="/register"
              class="btn btn-soft btn-primary w-full sm:w-auto"
              >Register for the next tournament 📝</a
            >
            <a
              role="button"
              href="https://photos.keshmat.org"
              class="btn btn-outline btn-secondary w-full sm:w-auto"
              target="_blank">Photos from previous tournaments 📸</a
            >
          </div>
        </div>
      </div>
    </section>
  </main>
</div>
